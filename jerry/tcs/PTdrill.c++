//Board does not change
#include<iostream>
#include<vector>
using namespace std;
int main() {
    int n,count=0;
    cin >> n;
    vector<int> board(n),jersey(n);
    for(int i=0;i<n;i++) {
        jersey[i] =i+1;
    }
    for(int i=0;i<n;i++) {
        cin >> board[i];
    }
    int no_balloons=
    return 0;
}