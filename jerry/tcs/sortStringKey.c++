#include<iostream>
#include<string>
using namespace std;
int main() {
    string str,key;
    getline(cin,str);
    getline(cin,key);
    int len=key.length();
    int x=0;
    for(int i=0;i<key.length();i++) {
        
    }

    return 0;
}